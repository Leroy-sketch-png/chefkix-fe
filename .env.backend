# ChefKix Frontend - Real Backend Configuration
# =============================================
# Copy this to .env.local to connect to Docker backend

# DISABLE mocking - use real API
NEXT_PUBLIC_API_MOCKING=disabled

# API Gateway URL (all API calls go through here)
NEXT_PUBLIC_BASE_URL=http://localhost:8888

# Frontend URL (for OAuth redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Test Credentials (for reference only - don't put in env):
# Username: testuser
# Password: test123

# Google OAuth Client ID (optional - leave empty if not using)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
