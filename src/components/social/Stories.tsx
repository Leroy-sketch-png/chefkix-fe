// Stories component — feature pending backend Stories API (GET /api/v1/stories).
// Returns null until the endpoint exists. Do NOT ship dead UI.
// When Stories goes live: replace this file with the real implementation.

interface StoriesProps {
	variant?: 'horizontal' | 'compact'
	showTitle?: boolean
	currentUserId?: string
}

export const Stories = (_props: StoriesProps) => null
