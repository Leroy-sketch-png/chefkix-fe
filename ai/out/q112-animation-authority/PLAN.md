# Q112 Plan - Canonical Animation Utility Authority

## Decision

Move six legacy Tailwind-v3 animation promises into the active CSS-first authority, normalize camelCase utility requests to kebab-case, preserve dormant reusable components, and make the live SVG border beam animate its actual dash geometry.

## Three-Pass Rinse

1. Epicenter: prove the six selectors are absent from fresh compiled CSS and add bounded canonical utilities.
2. Blast radius: trace live and dormant consumers, preserve component behavior, bind beam distance to `1000 + beamSize`, and verify premium/notification/rendered beam contracts.
3. Systemic sweep: enumerate all production animation tokens, reject camelCase recurrence, preserve Tailwind/plugin-owned utilities, and probe the final minified CSS for selectors, keyframes, beam property, and reduced-motion policy.

## Rejected Shortcut

Restoring SCOPE's proposed class names without inspecting the beam keyframe was insufficient: the existing keyframe animated `offset-distance`, while the SVG uses stroke dashes and has no offset path.

## Verification

Focused and adjacent Jest, full Jest, TypeScript, ESLint, Prettier, production build, compiled CSS probes, source/diff sweeps, and Agent OS.
