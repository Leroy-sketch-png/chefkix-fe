# Q110 Diff Record

## Cleared Cluster

- `[file:profile/recipe/collection callers]` dead aliases -> active canonical utilities.
- `[file:UserProfile.tsx]` fixed white plus theme-flipping text -> fixed dark overlay plus white text.
- `[file:design-token-contract.test.ts]` no alias/contrast guard -> systemic production-source guard.

## Structural Verification

- Seven callers now use canonical XP, streak, level, and combo utilities; valid semantic pink PageHeader callers are unchanged.
- The collection type pill uses a fixed foreground/background pair with calculated worst-case contrast.
- The production-source contract rejects both retired aliases and fixed-white/theme-text combinations.
- No palette value, component structure, layout, data, API, navigation, or interaction behavior changed.
